// ====================================================================================
// ARQUIVO DE PRESERVAÇÃO MASTER - TIVOR
// Este arquivo contém as models customizadas da TIVOR que não estão no db pull original.
// Se o "npx prisma db pull" apagar estas models, basta copiá-las de volta ao schema.prisma principal.
// ====================================================================================

// 1. Tabela de Captura de Leads (SITE_CONT)
// Descrição: Armazena leads da landing page institucional com auditoria forense.
model Lead {
  id        String   @id @default(uuid()) @map("UUID_CONT")
  name      String   @map("NOME_CONT")
  email     String   @map("MAIL_CONT")
  company   String?  @map("EMPR_CONT")
  message   String   @map("MENS_CONT")
  
  // Auditoria Técnica (Senior Master)
  ip        String?  @map("IP_CONT")
  userAgent String?  @map("UA_CONT")
  
  // Controle de AuditTrail
  createdAt DateTime @default(now()) @map("CREA_CONT")
  updatedAt DateTime @updatedAt @map("TIME_CONT")

  @@index([email], name: "idx_site_cont_mail")
  @@map("SITE_CONT")
}

// Futuras models customizáveis devem ser adicionadas aqui para preservação.
// Exemplo: SESS_COMERCIAL, DASH_LOGS, etc.
